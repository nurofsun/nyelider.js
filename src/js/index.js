class Vlider {
    constructor(options) {
        this.options = {
            size: 1,
            direction: 'horizontal',
            el: '.vlider'
        }
    }
    init() {
        let vlider = document.querySelector(el);
    }
}
